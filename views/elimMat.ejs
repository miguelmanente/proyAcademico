<!DOCTYPE html>
<html lang="es">

<body>
    <header>
        <%- include('../views/partials/header'); %>
    </header>
   
     <main>
        <div class="container-">
            <div class="row mt-3 ml-5 mr-5 ">
                <div class="col-md-12 text-center">
                    <h2>MATERIAS</h2> 
                    <table class="table table-bordered table-hover table-responsive">
                        <thead>
                            <tr>
                                <th class="border-primary">Nº</th>
                                <th class="border-primary">APELLIDO NOMBRE</th>
                                <th class="border-primary">DESCRIPCIÓN</th>
                                <th class="border-primary">ELIMINAR MATERIAS</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (data) { %>
                                <% for (var i=0; i < data.length; i++) { %>
                                    <tr>
                                        <td class="border-primary">
                                            <%= (i + 1) %>
                                        </td>
                                        <td class="border-primary">
                                            <%= data[i].nombre %>
                                        </td>
                                      
                                        <td class="border-primary">
                                            <%= data[i].descripcion %>
                                        </td>
                          
                                         <td class="border-primary">
                                            <a href="/elimMat/<%= data[i].id_materia %>" onclick="return confirm('¿Eliminas esta Materia?')">🗑️ Eliminar</a>
                                        </td>
                                    </tr>
                                <% } %>
                            <% } %>  
                        </tbody>
                    </table>
                    <a href="index" class="btn btn-info btn-sm rounded-pill">Volver al inicio</a>
                  </div>
             </div>
         </div>
      </main>  
</body>
</html>